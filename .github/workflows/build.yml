name: Build and Release Executables

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        
    - name: Build executable
      run: pyinstaller --onefile htm2pdf.py

    - name: Rename executable (Windows)
      if: matrix.os == 'windows-latest'
      run: Rename-Item -Path "dist/htm2pdf.exe" -NewName "htm2pdf-windows.exe"

    - name: Rename executable (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: mv dist/htm2pdf dist/htm2pdf-linux

    - name: Rename executable (macOS)
      if: matrix.os == 'macos-latest'
      run: mv dist/htm2pdf dist/htm2pdf-macos

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: executable-${{ matrix.os }}
        path: |
          dist/htm2pdf-linux
          dist/htm2pdf-macos
          dist/htm2pdf-windows.exe

    - name: Upload to Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: "v1.0.1"
        name: "Cross-Platform Release"
        files: |
          dist/htm2pdf-linux
          dist/htm2pdf-macos
          dist/htm2pdf-windows.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
